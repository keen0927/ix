// import { useCallback } from 'react';
// import { useSelector } from 'react-redux';
// import { RootState } from '../modules';

// const useCurrentId = () => {
//   // const currentListId = useSelector((state: RootState) => state.todos.currentListId);
//   // return currentListId;
//   return useCallback(() => useSelector((state: RootState) => state.todos.currentListId),[]);
// };

// export default useCurrentId;